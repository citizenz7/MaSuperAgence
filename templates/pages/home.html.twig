{% extends 'base.html.twig' %}

{% block title %}
    Agence Lorem ipsum
{% endblock %}

{% block body %}

<div class="jumbotron text-center">
    <h1>Agence Lorem Ipsum</h1>
    <p>Lorem, ipsum dolor sit amet consectetur adipisicing elit. Quisquam fuga voluptatem rerum nam perspiciatis
        maiores. Eligendi a distinctio sint voluptate quisquam maiores eos nihil, esse in iste similique ad
        asperiores.
    </p>
</div>

<div class="container">
    <h2>Les derniers biens</h2>
    <div class="row flex">
        {% for property in properties %}
            <div class="col-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            <a href="{{ path('property_show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                        </h5>
                        <p class="car-text">{{ property.city }} ({{ property.postalCode }})</p>
                        <div class="text-primary font-weight-bold lead">{{ property.formatedPrice }} €</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}